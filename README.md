# polyTube
A Chrome extension that replaces Polygon's default player with a YouTube embed if it exists.

##Installation instructions
1. Open Chrome's extensions page (chrome://extensions).
2. Drag and drop *polyTube.crx* into it.
